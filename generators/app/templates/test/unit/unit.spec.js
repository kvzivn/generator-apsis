// If you need angular, angular-mocks and Jasmine-Matchers, uncomment the import
// below.
// import '../helpers/imports';

// Import the file you are testing
// import { moduleName } from ''

// Below is a suggested structure for you unit tests. Modify as needed.
describe('MODULE NAME', () => {
    describe('when the module is exported it', () => {
        it('should export something', () => {
            expect(true).toEqual(false);
        });
    });

    describe('when the module has been instantiated it', () => {
        it('should execute something', () => {
            expect(true).toEqual(false);
        });
    });
});
